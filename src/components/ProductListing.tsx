import { Product } from '@/payload-types'
import React from 'react'
interface ProductListingProps {
  item:Product | null,
}
const ProductListing = ({item}: ProductListingProps) => {
  return (
  <>
  
  </>
  )
}

export default ProductListing